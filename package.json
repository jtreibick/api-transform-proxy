{
  "name": "api-transform-proxy",
  "private": true,
  "type": "module",
  "scripts": {
    "build:templates": "node scripts/build-templates.mjs",
    "bootstrap:master-key": "bash scripts/bootstrap-master-key.sh",
    "deploy:cf": "npm run bootstrap:master-key && npm exec wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.45.0"
  },
  "dependencies": {
    "jsonata": "^2.0.6",
    "yaml": "^2.8.1"
  }
}
